// Constants for cross-microfrontend shared state
export const THEMES = {
  LIGHT: 'light' as const,
  DARK: 'dark' as const,
}

export const USER_ROLES = {
  ADMIN: 'admin' as const,
  USER: 'user' as const,
}